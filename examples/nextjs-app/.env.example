# Server-side (instrumentation.ts, route handlers)
LOGSETU_API_KEY=ls_xxx
LOGSETU_ENDPOINT=http://localhost:8686
# Browser-side (client components) — same key works; prefix makes Next expose it to the browser
NEXT_PUBLIC_LOGSETU_API_KEY=ls_xxx
NEXT_PUBLIC_LOGSETU_ENDPOINT=http://localhost:8686
